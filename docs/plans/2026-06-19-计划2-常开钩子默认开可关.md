# 计划2：常开钩子（默认开 + 可关）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让插件**默认常开**——每次会话开始时，SessionStart 钩子自动把 rookie-work 的**紧凑核心纪律**（预导语）注入上下文；并实现**可关**——存在关闭标记时钩子改注入一行「已关闭」提示、不注入纪律。

**Architecture:** `hooks/hooks.json` 声明 SessionStart 钩子（matcher `startup|resume|clear|compact`，自动发现，无需写进 plugin.json）。钩子经跨平台包装器 `hooks/run-hook.cmd` 调用无扩展名 bash 脚本 `hooks/session-start`：脚本查关闭标记 → 读 `SESSION-PREAMBLE.md` → 用平台感知的 JSON 把文本放进 `hookSpecificOutput.additionalContext` 输出到 stdout。注入的是**紧凑预导语**（每会话都加载，保持小）；**完整方法**在计划 3 的 `SKILL.md`，由预导语指引按需加载。

**Tech Stack:** Claude Code 钩子系统（SessionStart / `additionalContext`）、bash、Windows cmd polyglot、`${CLAUDE_PLUGIN_ROOT}` 环境变量、JSON、`python3`（仅测试用，校验 JSON）。

---

## 前提与依赖

- **依赖计划 1** 的可安装骨架（仓库根、`.claude-plugin/`）。
- 注入文本分两层：**预导语**（本计划，紧凑、常驻）与**完整方法**（计划 3 的 `SKILL.md`）。预导语里出现的「load the `rookie-work` skill」在计划 3 落地前指向尚不存在的技能——属**前向引用**，不影响本计划测试（本计划只验证「钩子是否把预导语文本正确注入 / 关闭时是否抑制」）。

## 关键设计决策（请评审）

1. **关闭标记放插件目录之外**：全局 `~/.rookie-work-off`、项目级 `<project>/.rookie-work-off`，**任一存在即关闭**。原因：插件安装目录每次更新都会被清空（官方明确告诫不要把持久状态写进 `${CLAUDE_PLUGIN_ROOT}`）。官方推荐持久位是 `${CLAUDE_PLUGIN_DATA}`，但我选 `~/` 是为了**新手易发现易删除**。
2. **关闭时注入一行「已关闭」提示**（而非完全静默），让新手知道它装着但关了、以及怎么重开。若你更想要「关 = 完全静默」，评审时说，改成 off 分支直接 `exit 0` 不输出即可。
3. **跨平台包装器 `run-hook.cmd`**：默认采用（适配 Windows 找 Git Bash），polyglot 模式**沿用 superpowers（MIT）**并署名——这是通用工具垫片，与「不照搬技能正文」不冲突。
   **替代方案 B（更简单，去包装器）**：`hooks.json` 直接写 `"command": "bash", "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/session-start"]`。代价：Windows 新手需自带 `bash` 在 PATH。评审选 B 则删 `run-hook.cmd`、换 hooks.json 的 command（见 Task 2 备注）。

## 文件结构（本计划产出）

```
SESSION-PREAMBLE.md          # 每会话注入的紧凑核心纪律（插件根）
hooks/
  hooks.json                 # 声明 SessionStart 钩子
  run-hook.cmd               # 跨平台 polyglot 包装器（适配 Win/Unix）
  session-start              # bash：查关闭标记 → 读预导语 → 输出平台感知 JSON
tests/
  test-session-start.sh      # 钩子行为测试（默认注入 / 全局关 / 项目关）
```

---

### Task 1: 预导语 `SESSION-PREAMBLE.md`

**Files:**
- Create: `SESSION-PREAMBLE.md`

- [ ] **Step 1: 写 `SESSION-PREAMBLE.md`**

写入 `~/projects/rookie-work/SESSION-PREAMBLE.md`，内容：

```markdown
You are running under **rookie-work**, a guardrail that protects people new to AI agents. Follow these rules for the whole session, unless the user turns rookie-work off.

**Your promise to the user:** never change anything of theirs before telling them — in plain language they understand — what you are about to do and how, and getting their OK.

**Three principles**
1. **Explain before you act.** Before any real change, say in plain language what you will do and how, confirm the direction with the user, then act.
2. **No silent decisions.** When there is a real choice (which approach, which model for a sub-task, whether to run an extra review), lay out the options with pros and cons and a recommendation, and let the user pick. Use plain words; assume no jargon.
3. **Right-size the ceremony** using the three tiers below.

**Three tiers — choose one per task; for anything that changes things, say which tier and why**
- **Tier 0 — just do it:** read-only / look-up / locate / explain. Nothing changes. No ceremony — answer directly.
- **Tier 1 — light:** a small, low-risk change with one obvious way to do it. Say what you will do and how (plain language) → confirm direction → do it → report → stop before doing anything beyond what was asked.
- **Tier 2 — full:** real design space, wide blast radius, or hard to undo. Run the full rookie-work method (load the `rookie-work` skill): understand the project → brief it back and confirm → surface the boundary choices → design → plan → build → verify.

**Hard line:** read-only → just do it; the moment anything is written / changed / deleted / installed / published / committed → at least Tier 1, never silently skipped. Never talk yourself into "this is too simple" to skip the explanation.

The user can re-grade any task ("just do it" for lighter, "do this properly" for fuller) and can turn rookie-work off ("turn off rookie-work" or "raw mode").

**Always answer in the user's own language** — whatever they write in.

For a Tier 2 task, load the full method now via the `rookie-work` skill. For Tier 0 and Tier 1 you may proceed under the rules above.
```

- [ ] **Step 2: 验证含关键标记句**

Run: `grep -c "Explain before you act" ~/projects/rookie-work/SESSION-PREAMBLE.md`
Expected: 输出 `1`（测试将以此句判断「纪律是否被注入」）。

- [ ] **Step 3: 提交**

```bash
git -C ~/projects/rookie-work add SESSION-PREAMBLE.md
git -C ~/projects/rookie-work commit -m "feat: 加入每会话注入的紧凑核心纪律预导语"
```

---

### Task 2: 钩子声明 `hooks/hooks.json`

**Files:**
- Create: `hooks/hooks.json`

- [ ] **Step 1: 写 `hooks/hooks.json`（默认方案 A：经包装器）**

写入 `~/projects/rookie-work/hooks/hooks.json`，内容：

```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "async": false
          }
        ]
      }
    ]
  }
}
```

> **若评审选替代方案 B（去包装器）**，把上面 `hooks` 数组里那一项换成：
> ```json
> { "type": "command", "command": "bash", "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/session-start"], "timeout": 30 }
> ```
> 并在 Task 4 跳过 `run-hook.cmd` 的创建。

- [ ] **Step 2: 验证 JSON 合法且声明了 SessionStart**

Run:
```bash
python3 -c "import json;h=json.load(open('/Users/xuzheng/projects/rookie-work/hooks/hooks.json'));ss=h['hooks']['SessionStart'][0];assert 'startup' in ss['matcher'] and ss['hooks'][0]['type']=='command';print('hooks.json OK')"
```
Expected: 打印 `hooks.json OK`，无报错。

- [ ] **Step 3: 提交**

```bash
git -C ~/projects/rookie-work add hooks/hooks.json
git -C ~/projects/rookie-work commit -m "feat: 声明 SessionStart 钩子(hooks.json)"
```

---

### Task 3: 行为测试 `tests/test-session-start.sh`（先写，必失败）

**Files:**
- Create: `tests/test-session-start.sh`

- [ ] **Step 1: 写测试脚本**

写入 `~/projects/rookie-work/tests/test-session-start.sh`，内容：

```bash
#!/usr/bin/env bash
# Behavior tests for the rookie-work SessionStart hook script.
# Runs hooks/session-start in an ISOLATED env (temp HOME + temp project dir)
# so it never touches the real ~/.rookie-work-off.
set -uo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
SCRIPT="${REPO_ROOT}/hooks/session-start"
PASS=0; FAIL=0
ok()   { echo "PASS: $1"; PASS=$((PASS+1)); }
bad()  { echo "FAIL: $1"; FAIL=$((FAIL+1)); }

TMP="$(mktemp -d)"; trap 'rm -rf "$TMP"' EXIT
mkdir -p "$TMP/home" "$TMP/proj"

run_hook() {
  ( cd "$TMP/proj" && HOME="$TMP/home" CLAUDE_PLUGIN_ROOT="$REPO_ROOT" \
      CLAUDE_PROJECT_DIR="$TMP/proj" bash "$SCRIPT" </dev/null )
}

# 1) Default (no off-marker)
OUT="$(run_hook)"
if printf '%s' "$OUT" | python3 -m json.tool >/dev/null 2>&1; then ok "default: valid JSON"; else bad "default: valid JSON"; fi
if printf '%s' "$OUT" | grep -q "hookSpecificOutput"; then ok "default: uses hookSpecificOutput"; else bad "default: uses hookSpecificOutput"; fi
if printf '%s' "$OUT" | grep -q "Explain before you act"; then ok "default: injects discipline"; else bad "default: injects discipline"; fi

# 2) Global off-marker
touch "$TMP/home/.rookie-work-off"
OUT="$(run_hook)"
if printf '%s' "$OUT" | grep -q "currently OFF"; then ok "global off: shows OFF notice"; else bad "global off: shows OFF notice"; fi
if printf '%s' "$OUT" | grep -q "Explain before you act"; then bad "global off: suppresses discipline"; else ok "global off: suppresses discipline"; fi
rm -f "$TMP/home/.rookie-work-off"

# 3) Project off-marker
touch "$TMP/proj/.rookie-work-off"
OUT="$(run_hook)"
if printf '%s' "$OUT" | grep -q "currently OFF"; then ok "project off: shows OFF notice"; else bad "project off: shows OFF notice"; fi
if printf '%s' "$OUT" | grep -q "Explain before you act"; then bad "project off: suppresses discipline"; else ok "project off: suppresses discipline"; fi
rm -f "$TMP/proj/.rookie-work-off"

echo "----"; echo "PASS=$PASS FAIL=$FAIL"
[ "$FAIL" -eq 0 ]
```

- [ ] **Step 2: 赋可执行 + 运行（应失败）**

Run:
```bash
chmod +x ~/projects/rookie-work/tests/test-session-start.sh
bash ~/projects/rookie-work/tests/test-session-start.sh
```
Expected: **FAIL** —— 因 `hooks/session-start` 尚不存在，`bash "$SCRIPT"` 报错、`OUT` 为空，多条断言失败，末行 `PASS=0 FAIL=6`，退出码非 0。

- [ ] **Step 3: 提交（先提交失败的测试）**

```bash
git -C ~/projects/rookie-work add tests/test-session-start.sh
git -C ~/projects/rookie-work commit -m "test: 加入 SessionStart 钩子行为测试(当前必失败)"
```

---

### Task 4: 实现钩子脚本 `hooks/session-start` 与包装器 `hooks/run-hook.cmd`

**Files:**
- Create: `hooks/session-start`
- Create: `hooks/run-hook.cmd`

- [ ] **Step 1: 写 `hooks/session-start`**

写入 `~/projects/rookie-work/hooks/session-start`，内容：

```bash
#!/usr/bin/env bash
# rookie-work SessionStart hook: inject the compact discipline preamble,
# unless an off-switch marker is present.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PLUGIN_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

GLOBAL_OFF="${HOME}/.rookie-work-off"
PROJECT_DIR="${CLAUDE_PROJECT_DIR:-$PWD}"
PROJECT_OFF="${PROJECT_DIR}/.rookie-work-off"

if [ -f "$GLOBAL_OFF" ] || [ -f "$PROJECT_OFF" ]; then
  CONTEXT="rookie-work is installed but currently OFF (an off-switch marker is present). To turn it back on, remove the marker file: ${GLOBAL_OFF} (everywhere) or ${PROJECT_OFF} (this project only)."
else
  CONTEXT="$(cat "${PLUGIN_ROOT}/SESSION-PREAMBLE.md" 2>/dev/null || echo "rookie-work is active, but its preamble file is missing.")"
fi

# Escape a string for embedding inside a JSON string value.
escape_for_json() {
  local s="$1"
  s="${s//\\/\\\\}"
  s="${s//\"/\\\"}"
  s="${s//$'\n'/\\n}"
  s="${s//$'\r'/\\r}"
  s="${s//$'\t'/\\t}"
  printf '%s' "$s"
}
CTX="$(escape_for_json "$CONTEXT")"

# Emit platform-aware JSON. Claude Code consumes hookSpecificOutput.additionalContext;
# other SDK platforms consume top-level additionalContext.
if [ -n "${CLAUDE_PLUGIN_ROOT:-}" ] && [ -z "${COPILOT_CLI:-}" ]; then
  printf '{\n  "hookSpecificOutput": {\n    "hookEventName": "SessionStart",\n    "additionalContext": "%s"\n  }\n}\n' "$CTX"
else
  printf '{\n  "additionalContext": "%s"\n}\n' "$CTX"
fi
exit 0
```

- [ ] **Step 2: 写 `hooks/run-hook.cmd`（跨平台 polyglot，方案 A）**

> 选了替代方案 B 则跳过本步。

写入 `~/projects/rookie-work/hooks/run-hook.cmd`，内容：

```bat
: << 'CMDBLOCK'
@echo off
REM rookie-work cross-platform hook wrapper.
REM Polyglot pattern adapted from the superpowers project
REM (https://github.com/obra/superpowers), MIT License.
REM On Windows: cmd runs this batch block, finds Git Bash, calls the hook script.
REM On Unix: bash treats this block as a heredoc to ':' (discarded), runs the tail.
REM Usage: run-hook.cmd <script-name> [args...]
if "%~1"=="" (
    echo run-hook.cmd: missing script name >&2
    exit /b 1
)
set "HOOK_DIR=%~dp0"
if exist "C:\Program Files\Git\bin\bash.exe" (
    "C:\Program Files\Git\bin\bash.exe" "%HOOK_DIR%%~1" %2 %3 %4 %5 %6 %7 %8 %9
    exit /b %ERRORLEVEL%
)
if exist "C:\Program Files (x86)\Git\bin\bash.exe" (
    "C:\Program Files (x86)\Git\bin\bash.exe" "%HOOK_DIR%%~1" %2 %3 %4 %5 %6 %7 %8 %9
    exit /b %ERRORLEVEL%
)
where bash >nul 2>nul
if %ERRORLEVEL% equ 0 (
    bash "%HOOK_DIR%%~1" %2 %3 %4 %5 %6 %7 %8 %9
    exit /b %ERRORLEVEL%
)
REM No bash found - exit cleanly so the session still starts (just without injection).
exit /b 0
CMDBLOCK

# Unix path. (Wrapper pattern adapted from superpowers, MIT.)
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
SCRIPT_NAME="$1"
shift
exec bash "${SCRIPT_DIR}/${SCRIPT_NAME}" "$@"
```

- [ ] **Step 3: 赋可执行**

Run: `chmod +x ~/projects/rookie-work/hooks/session-start ~/projects/rookie-work/hooks/run-hook.cmd`
Expected: 无输出，退出码 0。

- [ ] **Step 4: 运行测试（应全过）**

Run: `bash ~/projects/rookie-work/tests/test-session-start.sh`
Expected: **PASS** —— 末行 `PASS=6 FAIL=0`，退出码 0。

- [ ] **Step 5: 验证包装器能转调脚本（Unix 路径）**

Run:
```bash
cd ~/projects/rookie-work && HOME="$(mktemp -d)" CLAUDE_PLUGIN_ROOT="$PWD" bash hooks/run-hook.cmd session-start </dev/null | python3 -m json.tool >/dev/null && echo "wrapper OK"
```
Expected: 打印 `wrapper OK`（包装器成功 exec `session-start`，输出合法 JSON）。

- [ ] **Step 6: 提交**

```bash
git -C ~/projects/rookie-work add hooks/session-start hooks/run-hook.cmd
git -C ~/projects/rookie-work commit -m "feat: 实现 SessionStart 钩子脚本与跨平台包装器(测试全过)"
```

---

### Task 5: 集成校验（无文件改动）

**Files:** 无（仅校验）

- [ ] **Step 1: 校验 hooks.json 命令路径与实际文件一致**

Run: `ls -la ~/projects/rookie-work/hooks/`
Expected: 含 `hooks.json`、`run-hook.cmd`、`session-start`；`run-hook.cmd` 与 `session-start` 均有可执行位（`-rwxr-xr-x`）。

- [ ] **Step 2: 用官方校验器复校整插件（若可用）**

Run: `cd ~/projects/rookie-work && claude plugin validate . --strict`
Expected: 通过、无 error（此时插件有钩子、无技能——技能留给计划 3，仍应校验通过）。
（无该命令则跳过，靠 Task 2/3/4 的 JSON 与行为测试已覆盖。）

- [ ] **Step 3: 确认关闭标记位于插件目录之外**

Run: `grep -n "rookie-work-off" ~/projects/rookie-work/hooks/session-start`
Expected: 命中的标记路径基于 `${HOME}` 与 `${CLAUDE_PROJECT_DIR:-$PWD}`，**不含** `${CLAUDE_PLUGIN_ROOT}`（确保持久状态不写进会被清空的插件目录）。

---

## 完成判据（Plan 2 Done）

- 存在 `SESSION-PREAMBLE.md`、`hooks/hooks.json`、`hooks/session-start`、`hooks/run-hook.cmd`、`tests/test-session-start.sh`。
- `tests/test-session-start.sh` 全过（`PASS=6 FAIL=0`）：默认注入预导语、全局关闭抑制、项目关闭抑制。
- 钩子脚本可执行；包装器能转调脚本输出合法 JSON。
- 关闭标记基于 `~/` 与项目目录，不写入插件目录。
- 全部改动已分提交（main 分支）。
- **效果：装上插件后，新会话自动加载核心纪律；建 `~/.rookie-work-off` 或 `<项目>/.rookie-work-off` 即可整体/按项目关闭。**

## 自审（写完即查，对照 spec）

- **spec 覆盖**：落地 §5「启用与关闭」（默认常开 = SessionStart 注入；持久关 = 标记文件；口头当场关由计划 3 的技能处理，已注明）与 §4 的钩子件；§2 三原则、§6 三档、§9 交底铁律以**紧凑预导语**形式进入每会话上下文。
- **占位扫描**：无 TBD/TODO；预导语里「load the `rookie-work` skill」是对计划 3 的真实前向引用、已说明；方案 B 是明确可选项、非占位。
- **类型/命名一致**：脚本名 `session-start`、包装器 `run-hook.cmd`、预导语 `SESSION-PREAMBLE.md`、标记 `.rookie-work-off`、JSON 字段 `hookSpecificOutput.additionalContext` / `hookEventName`，在 hooks.json、session-start、测试三处拼写一致；测试断言句「Explain before you act」「currently OFF」与脚本/预导语实际输出逐字一致。
- **顺序依赖**：依赖计划 1 的骨架；为计划 3（技能 + 口头关闭 + 完整方法）提供注入入口与前向引用。
- **平台**：Claude Code 走 `hookSpecificOutput` 分支；非 CC SDK 平台走顶层 `additionalContext` 分支；Windows 由 `run-hook.cmd` 找 Git Bash。
