# 计划4：发布到 GitHub + 端到端真机冒烟 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 rookie-work 作为公开插件发布到 GitHub，并从**干净环境真机安装**走通一遍——确认「一键安装 → 默认常开 → 三档行为 → 可关」端到端成立；顺带收口命令名校验/回退、版本号与 README。

**Architecture:** 单仓库即插件即市场，推到 `github.com/xuzheng1210/rookie-work`；用户 `/plugin marketplace add` + `/plugin install` 两行装好；SessionStart 钩子带来默认常开。本计划不新增功能，只做**发布**与**真机验收**。

**Tech Stack:** git、GitHub（`gh` CLI 或网页建库）、Claude Code 插件安装命令（`/plugin marketplace add`、`/plugin install`、`/plugin marketplace update`）。

---

## 前提与依赖

- **依赖计划 1/2/3 已全部执行落地**：仓库根有 `.claude-plugin/`、`hooks/`、`SKILL.md`、`SESSION-PREAMBLE.md`、`references/`、`tests/`，且 `tests/test-session-start.sh`、`tests/test-skill.sh` 均通过。
- **这是对外动作**：推公开仓库不可逆。Task 2 推送前**必须经用户确认**（仓库名、公/私、是否一并公开 `docs/`）。
- 用户 GitHub 用户名假设 `xuzheng1210`；不同则全局替换（同计划 1 参数）。

## 待用户确认的发布决策（Task 2 前敲定）

1. **仓库名 / 可见性**：`xuzheng1210/rookie-work`，public？
2. **是否公开 `docs/`**（spec + 4 份中文计划随仓库一起公开）。默认公开（它们是设计资料）；若不想公开，发布前把 `docs/` 移出或加 `.gitignore`（但会丢掉「计划随仓库」的可追溯，需权衡）。
3. **建库方式**：`gh` CLI 一键，还是网页手动建库后推送。

## 本计划改动的文件

```
.claude-plugin/plugin.json        # version 0.1.0 -> 1.0.0
.claude-plugin/marketplace.json   # 插件条目 version 0.1.0 -> 1.0.0
README.md                         # 关闭开关补持久标记说明 + 状态改 v1.0.0
（条件）SKILL.md -> skills/rookie-work/SKILL.md   # 仅当命令名校验需回退
```

---

### Task 1: 发布前收口（版本 + README）

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `README.md`

- [ ] **Step 1: 升版本号到 1.0.0**

在 `~/projects/rookie-work/.claude-plugin/plugin.json` 中，把
`"version": "0.1.0"` 改为 `"version": "1.0.0"`。

在 `~/projects/rookie-work/.claude-plugin/marketplace.json` 中，把 `plugins[0]` 里的
`"version": "0.1.0"` 改为 `"version": "1.0.0"`。

- [ ] **Step 2: README 补「持久关闭」说明 + 改状态**

在 `~/projects/rookie-work/README.md` 中，把整节
```
## Turn it off

Tell the agent "turn off rookie-work" (or "give me raw mode") any time, and it stands down for the current session.

*(A persistent, across-sessions off-switch ships with the activation hook — see `docs/`.)*
```
替换为：
```
## Turn it off

- **Just this session:** tell the agent "turn off rookie-work" (or "give me raw mode"); it stands down for the current session.
- **Persistently:** create an empty file `~/.rookie-work-off` to turn it off everywhere, or `<your-project>/.rookie-work-off` to turn it off for one project. Delete the file to turn it back on. (You can also just ask the agent to do this for you.)
```
并把
```
## Status

Early development (v0.1.0). Behavior is being built out plan-by-plan; see `docs/specs/` and `docs/plans/`.
```
替换为：
```
## Status

v1.0.0 — first public release. Design and implementation notes live in `docs/`.
```

- [ ] **Step 3: 最终本地校验（全绿才发布）**

Run:
```bash
bash ~/projects/rookie-work/tests/test-session-start.sh
bash ~/projects/rookie-work/tests/test-skill.sh
python3 -m json.tool ~/projects/rookie-work/.claude-plugin/plugin.json >/dev/null && echo plugin.json-ok
python3 -m json.tool ~/projects/rookie-work/.claude-plugin/marketplace.json >/dev/null && echo marketplace.json-ok
cd ~/projects/rookie-work && claude plugin validate . --strict 2>/dev/null || echo "validate skipped/unavailable"
grep -c '"version": "1.0.0"' ~/projects/rookie-work/.claude-plugin/plugin.json ~/projects/rookie-work/.claude-plugin/marketplace.json
```
Expected: 两个测试 `FAIL=0`；两个 `*-ok`；validate 通过或注明跳过；两个清单各命中一次 `1.0.0`。

- [ ] **Step 4: 提交**

```bash
git -C ~/projects/rookie-work add .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md
git -C ~/projects/rookie-work commit -m "release: v1.0.0 收口(版本号 + README 持久关闭说明)"
```

---

### Task 2: 发布到 GitHub（对外动作 — 推送前确认）

**Files:** 无（git remote + push）

- [ ] **Step 1: 与用户确认发布决策**

逐条确认上文「待用户确认的发布决策」1/2/3。**未确认不推送。**

- [ ] **Step 2: 建库并推送**

方式甲（`gh` CLI，已登录）：
```bash
cd ~/projects/rookie-work && gh repo create xuzheng1210/rookie-work --public --source=. --remote=origin --push
```
（若 `gh` 未登录：让用户在会话里跑 `! gh auth login` 完成交互登录，再执行上句。）

方式乙（网页建空库后推送）：先在 github.com 建空仓库 `rookie-work`，再：
```bash
git -C ~/projects/rookie-work remote add origin https://github.com/xuzheng1210/rookie-work.git
git -C ~/projects/rookie-work push -u origin main
```

- [ ] **Step 3: 确认远端到位**

Run: `git -C ~/projects/rookie-work remote -v && git -C ~/projects/rookie-work log --oneline origin/main -1`
Expected: `origin` 指向 `xuzheng1210/rookie-work`；`origin/main` 含最新提交。
另：浏览器打开仓库页，确认 `.claude-plugin/`、`hooks/`、`SKILL.md` 均在线可见。

---

### Task 3: 端到端真机安装冒烟（人工/交互）

**Files:** 无（在装了插件的真 Claude Code 会话里观察）

- [ ] **Step 1: 干净安装**

在 Claude Code 里依次执行：
```text
/plugin marketplace add xuzheng1210/rookie-work
/plugin install rookie-work@rookie-work-marketplace
```
Expected: 市场添加成功、插件安装成功，无报错。

- [ ] **Step 2: 验证「默认常开」**

新开一个会话（或 `/clear`）。
Expected: 会话一开始，agent 即处于 rookie-work 之下——能复述/遵循「先交底、不静默决定、三档」的核心纪律（即预导语已被 SessionStart 钩子注入）。

- [ ] **Step 3: 验证命令名解析（+ 回退收口）**

打开 `/` 菜单查找。
- 期望：出现 **`/rookie-work`**。→ 命令名 OK，跳到 Step 4。
- **若**解析成 `/rookie-work:rookie-work` 或不出现 → 执行回退：
  ```bash
  git -C ~/projects/rookie-work mv SKILL.md skills/rookie-work/SKILL.md
  # 若 git mv 因未跟踪报错，则普通 mkdir+mv 后 git add -A
  git -C ~/projects/rookie-work commit -m "fix: 改用子目录技能形态(命令名稳定)"
  git -C ~/projects/rookie-work push
  ```
  然后在 CC 里 `/plugin marketplace update` → 重新 `/plugin install` → 重查 `/` 菜单，命令应为 `/rookie-work:rookie-work`（功能不变；默认常开走钩子、不依赖命令名）。
  注意：子目录形态下 `tests/test-skill.sh` 的 `SKILL` 路径需相应改为 `skills/rookie-work/SKILL.md`——同提交一并改测试路径。

- [ ] **Step 4: 行为冒烟（套用计划 3 的清单）**

逐条核对（计划 3 Task 4 的 7 个场景）：Tier 0 直接做 / Tier 1 交底-确认-改-报告-记日志 / Tier 2 吃透-简报-确认-选择题-方案-计划（不先写码）/ 改动前必交底 / 「关掉 rookie-work」当场停用 / 模型策略可设默认。
Expected: 各场景行为符合；不符则回 `SKILL.md` / `SESSION-PREAMBLE.md` 调措辞、重推、`/plugin marketplace update`。

- [ ] **Step 5: 验证「可关」端到端**

```bash
touch ~/.rookie-work-off
```
新开会话 → Expected：rookie-work 不再注入纪律（仅「已关闭+怎么重开」一行）。然后：
```bash
rm ~/.rookie-work-off
```
新开会话 → Expected：恢复默认常开。

---

### Task 4: 打标签收尾

**Files:** 无（git tag）

- [ ] **Step 1: 打版本标签并推送**

```bash
git -C ~/projects/rookie-work tag v1.0.0
git -C ~/projects/rookie-work push origin v1.0.0
```
Expected: 标签 `v1.0.0` 推到远端。

- [ ] **Step 2: 终态确认**

Run: `git -C ~/projects/rookie-work status && git -C ~/projects/rookie-work log --oneline -8`
Expected: 工作区干净；历史含 spec、4 份计划、各实现提交、release 提交、（如有）回退提交。

---

## 完成判据（Plan 4 Done / 项目 Done）

- 版本号两处均 `1.0.0`；README 关闭开关含持久标记说明、状态为 v1.0.0。
- 仓库已推到 `github.com/xuzheng1210/rookie-work`，`.claude-plugin/`、`hooks/`、`SKILL.md` 在线可见。
- 从干净环境 `/plugin marketplace add` + `/plugin install` 成功。
- **默认常开**成立（新会话自动加载纪律）；**命令名**为 `/rookie-work`（或按回退为 `/rookie-work:rookie-work` 并已收口）。
- 行为冒烟 7 场景通过；**可关**端到端通过（建/删 `~/.rookie-work-off` 生效）。
- `v1.0.0` 标签已推。
- **rookie-work 作为一个面向新手的公开质量兜底插件，可被任何人一键安装使用。**

## 自审（写完即查，对照 spec）

- **spec 覆盖**：落地 §4「分发」（GitHub + 市场 + 一键装）与 §5「默认常开 + 可关」的**真机验收**；§15 待定项里「插件/市场/钩子精确格式」「持久关闭机制」在计划 1-3 已定、本计划做端到端确认；命令名校验+回退收口计划 3 留的口子在此闭合。
- **占位扫描**：无 TBD/TODO；`gh` 与网页两种建库方式是明确可选项；回退是带条件触发的真实步骤。
- **类型/命名一致**：安装命令 `rookie-work@rookie-work-marketplace` 与计划 1 的 `plugin.json`/`marketplace.json` 名称一致；关闭标记 `~/.rookie-work-off` 与计划 2/3 一致；版本 `1.0.0` 两清单一致。
- **对外动作守则**：推送公开仓库前置「用户确认」关卡（仓库名/可见性/是否公开 docs）。
- **顺序依赖**：依赖计划 1/2/3 全部落地；为终态发布。
