# 框架化前置阶段 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 rookie-work 的 Tier 2 加一段「框架化 → 边界发现」前置阶段：当外行用户提不出开发框架时，Agent 主动提议、对话式地把模糊需求变成用户看得懂并拍板的框架，再进设计。

**Architecture:** 不新增档位、不新建 skill——把现有 Tier 2 第 0/2 步加强（`SKILL.md`），常开层加一句触发（`SESSION-PREAMBLE.md`），方法细节（边界清单 + 多轮循环 + 便携模板）落到新参考 `references/framing-and-boundaries.md`。三家分发由 `build/build.sh`（已 glob 复制 `references/*.md`）自动同步到 `dist/codex`、`dist/hermes`；防漂移闸守一致。版本 `1.0.0 → 1.1.0`。

**Tech Stack:** Markdown（英文，与现有 SKILL/references 一致）、bash 测试（`tests/*.sh`，`ok`/`bad` 断言）、`build/build.sh`、`python3`/`git`。

---

## 关键事实（实现者必读，已核实）

- **`build/build.sh:20`** 用 `cp "${REFS}"/*.md` **整目录 glob 复制** `references/*.md` 到两家 dist。→ 新建 `references/framing-and-boundaries.md` **自动同步，不用改 build.sh**。
- **`tests/test-build.sh` 不用改**：第 19–21 行动态比对 `references/` 与 dist 的 `*.md` 集合（新参考自动纳入，仍相等）；第 31–33 行动态比对 `plugin.json` 版本 == codex 清单版本（bump 后两边同为 1.1.0，仍绿）。
- **`tests/test-skill.sh:29`** 是"文件存在"循环（非数量/集合断言）→ 加第三个参考不会误红，只需把它加进该循环。
- **`SKILL.md` / `references/*.md` 是英文**（`SESSION-PREAMBLE.md` 自述 "These instructions are in English for portability"）。新参考也**写英文**；便携模板正文给英文，并注明"按用户语言翻译呈现"。
- 标记串均用 `grep -qF` 命中；markdown `**bold**` 不影响子串匹配。
- **每个任务**改完源文件后都要 `bash build/build.sh` 重生成 dist，并跑全部 4 套测试，保证每次提交都连贯、闸全绿。
- 提交用中文 message + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 尾注（house style）。**直接在 main 上提交，不要 push**（用户自行决定何时推）。

## 文件结构（本计划产出）

```
references/framing-and-boundaries.md   # 新建：边界清单(10维) + 多轮循环 + 便携模板（英文）
SKILL.md                               # 改：第0步加触发提示；第2步升级为"Frame the work"
SESSION-PREAMBLE.md                    # 改：加一句框架化触发（精简层）
.claude-plugin/plugin.json             # 改：version 1.0.0 → 1.1.0
README.md                              # 改：Status → v1.1.0；+一条"会帮你搭框架"卖点
tests/test-skill.sh                    # 改：+1 参考存在断言、+3 参考内容断言、+2 SKILL 段落标记
tests/test-session-start.sh            # 改：+1 注入含框架化标记
dist/codex/**, dist/hermes/**          # 重跑 build 自动再生（不手改）
# tests/test-build.sh 不改；build/build.sh 不改
```

基线测试计数（动手前）：`test-skill=13`、`test-session-start=7`、`test-build=22`、`test-pre-llm-call=6`，均 `FAIL=0`。

---

### Task 1: 新参考 `references/framing-and-boundaries.md`（方法本体）

**Files:**
- Create: `references/framing-and-boundaries.md`
- Modify: `tests/test-skill.sh`（第 29 行循环 +1；末尾 echo 前加内容断言块 +3）

- [ ] **Step 1: 先写失败断言（改 `tests/test-skill.sh`）**

(a) 把新参考加进现有的"参考存在"循环。将：

```bash
for ref in "references/changelog-format.md" "references/model-and-review-policy.md"; do
```

改为：

```bash
for ref in "references/changelog-format.md" "references/model-and-review-policy.md" "references/framing-and-boundaries.md"; do
```

(b) 在 `echo "----"; echo "PASS=$PASS FAIL=$FAIL"` 这一行**之前**，插入内容断言块：

```bash
# Framing reference content (boundary checklist + portable template)
FB="${REPO_ROOT}/references/framing-and-boundaries.md"
for m in "Scope & non-goals" "Definition of done" "turn it into a development framework"; do
  if grep -qF "$m" "$FB" 2>/dev/null; then ok "framing ref has: $m"; else bad "framing ref has: $m"; fi
done
```

- [ ] **Step 2: 跑测试，确认新断言失败**

Run: `bash ~/projects/rookie-work/tests/test-skill.sh`
Expected: **FAIL** —— 末行 `PASS=13 FAIL=4`（原 13 仍过；新参考不存在 → 存在断言 1 失败 + 内容断言 3 失败）。

- [ ] **Step 3: 创建参考文件**

写入 `~/projects/rookie-work/references/framing-and-boundaries.md`（**逐字**，英文）：

```markdown
# Framing & boundary-finding (Tier 2, when the user can't give a framework)

Many people arrive with a goal but no development framework — they know the
result they want, not how to scope it. That's normal. Your job is to build that
framework *with* them, in plain language, so they understand and own it — **not**
to silently turn a vague request into a polished prompt full of your own
assumptions. A fancy prompt the user can't read is exactly what we're avoiding:
they should always understand what they're building and what each decision means.

Use this when a Tier 2 task is too vague or under-specified to act on. First
**offer** it (don't impose): *"This is a sizable task and the shape isn't pinned
down yet — want me to help you turn it into a clear plan first, in plain
language?"* If they already gave a clear framework, skip straight to the design.

## How to frame the work

1. **Reflect the goal back as a starting framework.** In a few plain sentences,
   say what you understand they want and the rough shape of the work, and ask
   them to correct it. The output is a shared understanding they can read — not a
   prompt for you to consume.
2. **Sweep the boundary checklist in the background.** Go through the dimensions
   below silently; surface only the ones that are relevant to this task and need
   the user's decision. Do **not** read the list out loud item by item — that
   turns help into an interrogation.
3. **Turn each relevant boundary into a plain-language choice** — including the
   ones the user didn't know to raise. Give the options, the plain trade-offs,
   your recommendation and why, and let them pick. Never settle a boundary
   silently.
4. **Converge over several rounds** (see the loop below). One pass rarely settles
   a real project.

## The boundary checklist

A reusable starting set — not exhaustive. Add or drop dimensions per task.

1. **Scope & non-goals** — what it should do; and explicitly what it should *not* do.
2. **Who uses it & how** — the people who use it and the situations they use it in.
3. **Inputs -> outputs** — what goes in, what comes out.
4. **Data shape & persistence** — what the data looks like, where it lives, whether history is kept.
5. **Scale** — a handful or millions; how many users; how often.
6. **When things go wrong** — errors, empty data, bad input — what should happen.
7. **Integrations & dependencies** — external systems, accounts, third-party services.
8. **Where it runs** — local / web / phone / server, and anything that constrains that.
9. **Definition of done** — what "finished" means, how it's verified, who sees it.
10. **Hard constraints** — budget, time, privacy/compliance, things that must not change.

## The multi-round convergence loop

After each round, before moving on:

- **Recap the settled boundaries** in plain language.
- **Check they actually understand** each one — not just that they nodded.
- **Ask what else they want to pin down.**
- **Point out dimensions still open** from the checklist that you think matter.

Keep looping until the user is satisfied *and* you have no important dimension
left unflagged. Only then move on to the design step.

## A portable framing prompt (for use without rookie-work)

When the user wants to do this themselves in a plain AI tool, offer them this
template to paste — **in their own language** (translate it for them):

> Here's my rough idea:
> [write your need here, however simple]
>
> I may not know much about development, so please don't start building yet.
> First help me turn it into a development framework I can understand: go through
> these aspects with me, one at a time (ask one thing at a time, in plain words,
> give me options and your recommendation with the reason, and don't decide
> anything silently for me): scope & non-goals / who uses it & how / inputs &
> outputs / data & persistence / scale / what happens when things go wrong /
> integrations & dependencies / where it runs / definition of done / hard
> constraints. After each round, recap the boundaries we've settled, check I
> really understand them, then ask what else I want to pin down. Once the
> framework is mostly settled, move on to the actual design.
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bash ~/projects/rookie-work/tests/test-skill.sh`
Expected: **PASS** —— 末行 `PASS=17 FAIL=0`（13 + 参考存在 1 + 内容 3）。

- [ ] **Step 5: 重建 dist 并验防漂移**

Run: `bash ~/projects/rookie-work/build/build.sh && bash ~/projects/rookie-work/tests/test-build.sh`
Expected: build 打印 `build: regenerated ...`；test-build 末行 `PASS=22 FAIL=0`（动态 refs-set 现含 `framing-and-boundaries.md`，canon 与两家 dist 仍相等）。

- [ ] **Step 6: 全套测试 + 看 dist 增量**

Run: `for t in skill session-start build pre-llm-call; do bash ~/projects/rookie-work/tests/test-$t.sh | tail -1; done`
Expected: 依次 `PASS=17 FAIL=0` / `PASS=7 FAIL=0` / `PASS=22 FAIL=0` / `PASS=6 FAIL=0`。
Run: `git -C ~/projects/rookie-work status --short`
Expected: 新增 `references/framing-and-boundaries.md`、改动 `tests/test-skill.sh`，dist 下两处新增 `dist/codex/.../references/framing-and-boundaries.md` 与 `dist/hermes/.../references/framing-and-boundaries.md`。

- [ ] **Step 7: 提交**

```bash
git -C ~/projects/rookie-work add references/framing-and-boundaries.md tests/test-skill.sh dist/
git -C ~/projects/rookie-work commit \
  -m "feat: 新参考 framing-and-boundaries(边界清单+多轮循环+便携模板) + skill 测试" \
  -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `SKILL.md` 升级（第 0 步触发 + 第 2 步 Frame the work）

**Files:**
- Modify: `SKILL.md`（第 0 步硬线后加段；第 2 步整句替换）
- Modify: `tests/test-skill.sh`（第 24 行段落标记循环 +2）

- [ ] **Step 1: 先写失败断言（改 `tests/test-skill.sh`）**

把第 24 行的标记循环：

```bash
for m in "Which tier is this task" "the light flow" "the full method" "Disclose before you modify" "Record every change" "Turning rookie-work off and on"; do
```

改为（追加两个新标记）：

```bash
for m in "Which tier is this task" "the light flow" "the full method" "Disclose before you modify" "Record every change" "Turning rookie-work off and on" "Frame the work" "boundary checklist"; do
```

- [ ] **Step 2: 跑测试，确认新断言失败**

Run: `bash ~/projects/rookie-work/tests/test-skill.sh`
Expected: **FAIL** —— 末行 `PASS=17 FAIL=2`（`Frame the work`、`boundary checklist` 尚不在 `SKILL.md`）。

- [ ] **Step 3a: 第 0 步加触发提示（改 `SKILL.md`）**

定位第 0 步硬线段落结尾这句（唯一）：

```
If unsure between two tiers, pick the higher one or ask.
```

替换为（原句 + 新增一段）：

```
If unsure between two tiers, pick the higher one or ask.

**When the user can't give a clear framework (Tier 2):** if a task is Tier 2 but the request is too vague to act on — common when the user is new to development and only has an end-goal in mind — don't start guessing, and don't silently inflate it into a polished prompt. First **offer** to build the framework together: *"This is a sizable task and the shape isn't pinned down yet — want me to help you turn it into a clear plan first, in plain language?"* If they accept, do it in Step 2 of the full method. If they already gave a clear framework, proceed normally.
```

- [ ] **Step 3b: 第 2 步整句替换（改 `SKILL.md`）**

定位现第 2 步整句（唯一，以 `**2. Restate the task` 开头）：

```
**2. Restate the task and surface the decisions.** Say back what you think they want. Find the points where there's a real choice — and **don't decide silently.** For each, present the options in plain language with pros and cons and **your recommendation and why**, and let them pick.
```

替换为：

```
**2. Frame the work, then surface the decisions.** Say back what you think they want. **If the user couldn't give a clear framework, build one with them first** (see `references/framing-and-boundaries.md`): reflect their goal back as a plain-language starting framework and confirm it. Then sweep the **boundary checklist** in the background and surface only the boundaries that matter for this task — including the ones the user didn't know to raise. Turn each real choice into options in plain language with pros and cons and **your recommendation and why**, and let them pick; **don't decide silently.** Real projects don't settle in one pass: after each round, recap the settled boundaries, check the user actually understands them, ask what else to pin down, and flag any important dimension still open. Loop until the framework is clear, then design.
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bash ~/projects/rookie-work/tests/test-skill.sh`
Expected: **PASS** —— 末行 `PASS=19 FAIL=0`（17 + 2 新标记）。

- [ ] **Step 5: 重建 dist 并验防漂移**

Run: `bash ~/projects/rookie-work/build/build.sh && bash ~/projects/rookie-work/tests/test-build.sh`
Expected: test-build `PASS=22 FAIL=0`（两家 `SKILL.md == canonical` 仍真——dist 已随重建更新）。

- [ ] **Step 6: 全套测试**

Run: `for t in skill session-start build pre-llm-call; do bash ~/projects/rookie-work/tests/test-$t.sh | tail -1; done`
Expected: `PASS=19 FAIL=0` / `PASS=7 FAIL=0` / `PASS=22 FAIL=0` / `PASS=6 FAIL=0`。

- [ ] **Step 7: 提交**

```bash
git -C ~/projects/rookie-work add SKILL.md tests/test-skill.sh dist/
git -C ~/projects/rookie-work commit \
  -m "feat: SKILL 第0步触发 + 第2步 Frame the work(冷启动框架化+边界清单+多轮收敛)" \
  -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `SESSION-PREAMBLE.md` 加框架化触发（精简层）

**Files:**
- Modify: `SESSION-PREAMBLE.md`（硬线后加一句）
- Modify: `tests/test-session-start.sh`（默认块 +1 断言）

- [ ] **Step 1: 先写失败断言（改 `tests/test-session-start.sh`）**

在默认块的这一行（第 25 行附近）：

```bash
if printf '%s' "$OUT" | grep -q "Explain before you act"; then ok "default: injects discipline"; else bad "default: injects discipline"; fi
```

**之后**插入一行：

```bash
if printf '%s' "$OUT" | grep -q "build the framework"; then ok "default: injects framing trigger"; else bad "default: injects framing trigger"; fi
```

- [ ] **Step 2: 跑测试，确认新断言失败**

Run: `bash ~/projects/rookie-work/tests/test-session-start.sh`
Expected: **FAIL** —— 末行 `PASS=7 FAIL=1`（preamble 尚无 "build the framework"）。

- [ ] **Step 3: 改 `SESSION-PREAMBLE.md`**

定位硬线段落结尾这句（唯一）：

```
Never talk yourself into "this is too simple" to skip the explanation.
```

替换为（原句 + 新增一句）：

```
Never talk yourself into "this is too simple" to skip the explanation.

**If the user can't give a clear framework (Tier 2):** when a request is too vague to act on — common for people new to development — don't guess; first *offer* to build the framework together in plain language before designing (load the `rookie-work` skill for how).
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bash ~/projects/rookie-work/tests/test-session-start.sh`
Expected: **PASS** —— 末行 `PASS=8 FAIL=0`。

- [ ] **Step 5: 重建 dist 并验防漂移**

Run: `bash ~/projects/rookie-work/build/build.sh && bash ~/projects/rookie-work/tests/test-build.sh`
Expected: test-build `PASS=22 FAIL=0`（两家 `SESSION-PREAMBLE.md == canonical` 与 hermes `agent-hooks/SESSION-PREAMBLE.md == canonical` 仍真）。

- [ ] **Step 6: 全套测试**

Run: `for t in skill session-start build pre-llm-call; do bash ~/projects/rookie-work/tests/test-$t.sh | tail -1; done`
Expected: `PASS=19 FAIL=0` / `PASS=8 FAIL=0` / `PASS=22 FAIL=0` / `PASS=6 FAIL=0`。

- [ ] **Step 7: 提交**

```bash
git -C ~/projects/rookie-work add SESSION-PREAMBLE.md tests/test-session-start.sh dist/
git -C ~/projects/rookie-work commit \
  -m "feat: 常开 preamble 加框架化触发(Tier2 需求模糊→主动提议搭框架) + 钩子测试" \
  -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 版本 1.1.0 + README（发布收尾，机械步骤）

> **为何本任务无"先写失败测试"：** 版本传播已被 `test-build.sh:31-33` 的**动态**断言（`plugin.json` 版本 == codex 清单版本）守住——bump 后重跑 build，两边同为 `1.1.0`，断言仍绿并打印新版本号。再硬编码一个"等于 1.1.0"的断言只会与动态断言重复、且每次升级都要维护，是更差的测试设计。README 是文档、无测试。故本任务=改动 + 重建 + 跑绿验证。

**Files:**
- Modify: `.claude-plugin/plugin.json`（version）
- Modify: `README.md`（Status + 一条卖点）

- [ ] **Step 1: bump 版本（改 `.claude-plugin/plugin.json`）**

将：

```json
  "version": "1.0.0",
```

改为：

```json
  "version": "1.1.0",
```

- [ ] **Step 2: 改 `README.md`**

(a) 在 "What it does" 列表的最后一条（`- **Leaves a trail.**...why.`）**之后**，加一条卖点：

```markdown
- **Gets you started even if you can't write a spec.** If you only know the result you want, it offers to build the development framework *with* you first — in plain language, one decision at a time — so you understand and own what you're building before any code.
```

(b) 将 Status 段：

```markdown
## Status

v1.0.0 — first public release. Design and implementation notes live in `docs/`.
```

改为：

```markdown
## Status

v1.1.0 — adds a framing & boundary-finding front-stage to Tier 2 (helps users new to development turn a vague idea into a clear, owned plan before any code). v1.0.0 was the first public release. Design and implementation notes live in `docs/`.
```

- [ ] **Step 3: 重建 dist 并验版本传播**

Run: `bash ~/projects/rookie-work/build/build.sh && bash ~/projects/rookie-work/tests/test-build.sh | grep -E "version|PASS="`
Expected: 出现 `PASS: codex: version matches CC (1.1.0)` 与末行 `PASS=22 FAIL=0`。
Run: `python3 -c "import json;print(json.load(open('/Users/xuzheng/projects/rookie-work/dist/codex/.codex-plugin/plugin.json'))['version'])"`
Expected: `1.1.0`。

- [ ] **Step 4: 全套测试**

Run: `for t in skill session-start build pre-llm-call; do bash ~/projects/rookie-work/tests/test-$t.sh | tail -1; done`
Expected: `PASS=19 FAIL=0` / `PASS=8 FAIL=0` / `PASS=22 FAIL=0` / `PASS=6 FAIL=0`。

- [ ] **Step 5: 提交**

```bash
git -C ~/projects/rookie-work add .claude-plugin/plugin.json README.md dist/
git -C ~/projects/rookie-work commit \
  -m "release: v1.1.0(框架化前置阶段) + README 卖点/状态" \
  -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 完成判据（Plan Done）

- 新参考 `references/framing-and-boundaries.md` 存在，含 10 维边界清单 + 多轮循环 + 便携模板；并经 build 同步到 `dist/codex`、`dist/hermes` 的 `skills/rookie-work/references/`。
- `SKILL.md` 第 0 步有触发提示、第 2 步为 "Frame the work"（含冷启动框架化、边界清单、多轮收敛，并指向新参考）。
- `SESSION-PREAMBLE.md` 含框架化触发句（常开层只 +1 句）。
- 版本 `1.1.0`（`plugin.json` 单源 → codex 清单一致）；README Status/卖点更新。
- 四套测试全绿：`test-skill=19`、`test-session-start=8`、`test-build=22`、`test-pre-llm-call=6`，均 `FAIL=0`；重跑 `build/build.sh` 后 `git diff dist/` 干净（确定性）。
- 4 个提交在 main 上，**未 push**（交回控制者/用户决定）。

## 自审（对照 spec，写完即查）

- **spec 覆盖**：§5.1 触发→Task2 第0步 + Task3 preamble；§5.2 冷启动框架化→参考"How to frame"+SKILL 第2步；§5.3 边界清单(10维)→参考"The boundary checklist"；§5.4 多轮循环→参考"convergence loop"+SKILL 第2步；§5.5 便携模板→参考"portable framing prompt"；§6 落点→Task2/3；§7 打包/版本→Task1 自动同步 + Task4 版本；§8 测试→各任务 TDD（并据实测把 test-build 定为"无需改"）；§9 决策→设计照搬；§11 风险（不照念清单、收敛判据、preamble 体积）→参考与 SKILL 文案显式写明"background / don't read out loud"、"until satisfied and no dimension unflagged"、preamble 只加一句。
- **占位扫描**：无 TBD；参考全文、SKILL/preamble 替换文本、测试断言、版本/README 改动均为逐字内容与精确命令。
- **类型/命名一致**：标记串 `Frame the work` / `boundary checklist` / `Scope & non-goals` / `Definition of done` / `turn it into a development framework` / `build the framework` 在"被测内容"与"测试断言"两处**逐字一致**；文件名 `references/framing-and-boundaries.md` 在 Task1 创建、Task2 指向、Task1 测试三处一致；计数链 13→17→19、7→8、build 恒 22、pre-llm 恒 6 自洽。
- **不回归**：现有 SKILL 段落标记（"the full method" 等）、`test-build` 动态断言、off 开关行为均未触动；仅追加。
- **顺序依赖**：Task1（参考）先于 Task2（SKILL 指向它）；Task3、Task4 独立可后置。
