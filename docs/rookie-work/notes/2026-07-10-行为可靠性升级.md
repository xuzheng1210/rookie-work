# 行为可靠性升级实施记录

## 为什么做

把“真正选择必须由用户决定”从原则性描述升级为可解释、可测试、三平台同源的行为协议。

## 采用方案

采用“行为协议为主，示例与场景为辅”的混合方案。常驻 preamble 只保留硬规则，`SKILL.md` 保存正式流程，`references/decision-protocol.md` 保存完整协议和示例。

## 实际改动

- 行为协议与正反场景：完成。
- `SKILL.md` 全阶段接入：完成。
- SessionStart 首响应硬门：完成。
- `PROMPT-GATE.md` 单一来源、Claude/Codex `UserPromptSubmit` 逐提示提醒：完成。
- Hermes 首轮完整 preamble、后续轮次短 gate 注入：完成。
- Codex/Hermes 分发产物重建：完成。
- 版本、安装方式、发布配置：未修改。

## 自动验证

| Test | Result |
|---|---|
| `tests/test-skill.sh` | exit 0; `PASS=31 FAIL=0` |
| `tests/test-decision-protocol.sh` | exit 0; `PASS=46 FAIL=0` |
| `tests/test-session-start.sh` | exit 0; `PASS=15 FAIL=0` |
| `tests/test-build.sh` | exit 0; `PASS=30 FAIL=0` |
| `tests/test-pre-llm-call.sh` | exit 0; `PASS=14 FAIL=0` |
| `tests/test-user-prompt-submit.sh` | exit 0; `PASS=9 FAIL=0` |

自动测试全部通过后，状态为“实现完成”。

## 真实 Agent 验证

| Platform | Status | Evidence |
|---|---|---|
| Claude Code | 待真机验证 | 旧版 SessionStart-only 规则在默认模型 `deepseek-v4-pro` 上有三次保留的 `LIVE-01` 失败；2026-07-12 逐提示软门完成后，环境安全策略在进程创建前禁止外部调用，当前修复未获得新真机结果 |
| Codex | 待真机验证 | CLI 0.143.0；配置默认模型 `gpt-5.6-sol` / `xhigh`；当前构建隔离加载成功，但环境安全策略禁止把插件正文发送给外部模型，未启动模型会话 |
| Hermes | 待真机验证 | CLI 0.18.0；配置默认 provider 为 `deepseek`；当前构建隔离加载成功，但环境安全策略禁止把插件正文发送给外部模型，未启动模型会话 |

### 不可删除的失败记录

- Claude Code `LIVE-01` 尝试 1，session `e73fe747-8755-4d9c-b49f-fa9f4ddc43c3`：回复要先查看项目结构和登录代码，没有识别真正选择，也没有询问确认节奏。
- Claude Code `LIVE-01` 尝试 2，session `8b75bdc7-87c8-488f-a24b-4e84e89d12c0`：回复要探索项目并尝试调用 Explore 子任务，同样没有升级 Tier 2 或询问确认节奏。
- 调试日志确认插件、skill、SessionStart hook 与 2901 字符 preamble 均成功加载；因此上述结果记为行为失败，不记为加载故障。
- Claude Code `LIVE-01` 修复后尝试，session `0cee48f6-0560-4a00-a3bd-4834e771a5cb`：增加首响应硬门、`DP-15` 正反场景并通过 124 项确定性检查后，默认模型仍回复要先查看项目并尝试 Bash；说明仅强化 SessionStart 文案无效。
- 2026-07-12 增加逐提示软门、`DP-16` 正反场景，初始实现阶段通过 145 项确定性检查；独立审查修复完成后，当前总计 175 项确定性检查通过。新的 Claude `LIVE-01` 在进程创建前被环境安全策略拒绝，即使用户明确批准也不得重试或绕过，因此当前修复保持“待真机验证”。

不得把“验证失败”或“待真机验证”改写为通过。后续修复后的连续通过记录必须保留这些历史失败。

## 未包含

本轮只在功能分支工作；未推送、未发布、未安装、未调整版本，未启动全项目质量升级。
