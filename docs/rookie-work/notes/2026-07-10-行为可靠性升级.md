# 行为可靠性升级实施记录

## 为什么做

把“真正选择必须由用户决定”从原则性描述升级为可解释、可测试、三平台同源的行为协议。

## 采用方案

采用“行为协议为主，示例与场景为辅”的混合方案。常驻 preamble 只保留硬规则，`SKILL.md` 保存正式流程，`references/decision-protocol.md` 保存完整协议和示例。

## 实际改动

- 行为协议与正反场景：完成。
- `SKILL.md` 全阶段接入：完成。
- SessionStart 与 Hermes 首轮硬规则注入：完成。
- Codex/Hermes 分发产物重建：完成。
- 版本、安装方式、发布配置：未修改。

## 自动验证

| Test | Result |
|---|---|
| `tests/test-skill.sh` | exit 0; `PASS=28 FAIL=0` |
| `tests/test-decision-protocol.sh` | exit 0; `PASS=40 FAIL=0` |
| `tests/test-session-start.sh` | exit 0; `PASS=13 FAIL=0` |
| `tests/test-build.sh` | exit 0; `PASS=26 FAIL=0` |
| `tests/test-pre-llm-call.sh` | exit 0; `PASS=10 FAIL=0` |

自动测试全部通过后，状态为“实现完成”。

## 真实 Agent 验证

| Platform | Status | Evidence |
|---|---|---|
| Claude Code | 未执行 | 等待单独授权与可用环境 |
| Codex | 未执行 | 等待单独授权与可用环境 |
| Hermes | 未执行 | 等待单独授权与可用环境 |

不得把“未执行”改写为通过。

## 未包含

已按用户逐项授权在功能分支创建本地提交；未推送、未发布、未安装、未调整版本，未启动全项目质量升级。
